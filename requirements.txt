# requirements.txt — consolidado de requirements/fase1.txt..fase6.txt
# Generado para instalar todo el proyecto en un solo entorno virtual.
# Si una fase necesita una versión nueva, actualizá también su requirements/faseX.txt.

torch
torchvision
pillow
boto3
matplotlib>=3.7.0
scikit-learn>=1.2.0
numpy>=1.24.0
tqdm
scipy>=1.10.0
deepface>=0.0.79
tf-keras
wandb>=0.15.0
anthropic
langdetect
transformers<5
sentencepiece
fastapi
uvicorn[standard]
python-multipart
gradio        # solo para la UI legacy (ui/legacy_gradio_app.py); la UI actual es web/ (React/PWA)
requests
python-dotenv
